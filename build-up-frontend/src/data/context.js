import { createContext } from 'react';

export const ProductContext = createContext(null);
export const PaymentSelectionContext = createContext(null);
export const ShippingAddressSelectionContext = createContext(null);
